[project]
name = "canceled-orders"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "bs4>=0.0.2",
    "fast-depends>=2.4.12",
    "gspread>=6.2.1",
    "httpx>=0.28.1",
    "lxml>=6.0.0",
    "structlog>=25.4.0",
]
